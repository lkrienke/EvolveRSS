<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ll_news_cell"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/selector_default"
    android:clickable="true"
    android:gravity="center_vertical"
    android:orientation="horizontal">

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/cell_image"
        android:layout_width="@dimen/image_length"
        android:layout_height="@dimen/image_length"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="8dp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="8dp">

        <TextView
            android:id="@+id/cell_title"
            android:layout_width="match_parent"
            android:layout_height="@dimen/title_line_height"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/title"
            android:textSize="@dimen/title"
            tools:text="Test title" />

        <TextView
            android:id="@+id/cell_description"
            android:layout_width="match_parent"
            android:layout_height="@dimen/description_line_height"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="@color/description"
            android:textSize="@dimen/description"
            tools:text="long description wowowowow omg what a long description I love it amazing this is the greatest wow what if the description is super long what will we do" />

    </LinearLayout>

</LinearLayout>